[package]
name = "expm_rust"
version = "0.1.0"
edition = "2024"
license = "BSD-3-Clause OR Apache-2.0"

[dependencies]
oxiblas = { version = "0.2", features = ["max-simd-256"], optional = true }

[dev-dependencies]
oxiblas = { version = "0.2", features = ["max-simd-256"] }


[features]
default = ["oxiblas-backend"]
oxiblas-backend = ["dep:oxiblas"]

[profile.dev]
debug = "line-tables-only"

[profile.release]
lto = "fat"
panic = "abort"
codegen-units = 1

[profile.test]
debug = "line-tables-only"
